package net.minecraft.client.renderer.entity;

import net.minecraft.entity.Entity;

/**
 * Custom entity compile stub: shape-only 1.16.5 (MCP) vanilla API used
 * by {@code forge/} sources. Never runs (compile classpath only).
 * Hierarchy flattened to the slot the factory signature needs
 * ({@code EntityRenderer<? super T>} — the real pig renderer renders
 * {@code PigEntity} through a longer chain, measured notch {@code egd}
 * extends a {@code <ban, ...>} renderer with ban the pig per
 * joined.tsrg). A dedicated server never loads this (the referencing
 * method is client-stripped, hub decisions/SPAWN.md).
 */
public class EntityRenderer<T extends Entity> {
}
