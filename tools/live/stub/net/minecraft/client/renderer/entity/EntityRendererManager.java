package net.minecraft.client.renderer.entity;

/**
 * Custom entity compile stub: shape-only 1.16.5 (MCP) vanilla API used
 * by {@code forge/} sources. Never runs (compile classpath only). Only
 * a type token for the renderer factory slot — the real manager lives
 * on the client, a dedicated server never loads this (the referencing
 * method is client-stripped, hub decisions/SPAWN.md).
 */
public class EntityRendererManager {
}
