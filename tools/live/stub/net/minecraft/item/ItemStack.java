package net.minecraft.item;

/**
 * Loot compile stub: shape-only 1.16.5 (MCP) vanilla API used by
 * {@code forge/} sources. Never runs (compile classpath only). Ctor
 * measured via javap against the pinned 36.2.42 bytes (public
 * {@code (Item,int)}); {@code getItem} is {@code func_77973_b} per the
 * pinned snapshot (kept for the companion tranche, unused by forge/ at
 * this tranche). Pinned by tools/run-live.sh at live time — drift fails
 * loudly.
 */
public class ItemStack {
    public ItemStack(Item item, int size) {
    }

    public ItemStack(Item item) {
    }

    public Item getItem() {
        return null;
    }
}
