package net.minecraft.item;

/**
 * Loot compile stub: the diamond carrier item. Non-final by design (a
 * final would fold into prod bytes — hub decisions/REPOP_SPIKE.md
 * no-stub-const lesson). Never runs. MCP name measured, not recalled:
 * {@code DIAMOND} is {@code field_151045_i} per the pinned MCP snapshot
 * 20210309 (same SRG as the 1.12 lowercase {@code diamond} — the case
 * changed between snapshots). Pinned by tools/run-live.sh (narrow map)
 * — drift fails loudly.
 */
public class Items {
    public static Item DIAMOND;
}
