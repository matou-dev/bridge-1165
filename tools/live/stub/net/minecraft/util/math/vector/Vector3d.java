package net.minecraft.util.math.vector;

/**
 * Combat compile stub: 36.2.42 look-vector component type. The bridge
 * combat hook reads the attacker look through {@code Entity.getLookVec}
 * and takes its components here — public final double fields
 * ({@code field_72450_a/b/c = x/y/z}, measured via javap against the
 * pinned notch server jar, obf {@code dcn/b/c/d}). Never runs (compile
 * classpath only).
 */
public class Vector3d {
    public double x;
    public double y;
    public double z;
}
