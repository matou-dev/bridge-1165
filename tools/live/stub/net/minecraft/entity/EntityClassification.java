package net.minecraft.entity;

/**
 * Custom entity compile stub: shape-only 1.16.5 (MCP) vanilla API used
 * by {@code forge/} sources. Never runs (compile classpath only). Only
 * a type token: the beast {@code Builder} classifies CREATURE like the
 * vanilla pig (measured in the pinned 36.2.42 bytes — the PIG block
 * builds under CREATURE). Enum constants ship MCP-named in the pinned
 * joined.tsrg (no SRG rename), so the runtime name is identical and no
 * reobfuscation row is needed (passthrough by construction, same as
 * Forge classes).
 */
public enum EntityClassification {
    MONSTER, CREATURE, AMBIENT, WATER_CREATURE, WATER_AMBIENT, MISC
}
