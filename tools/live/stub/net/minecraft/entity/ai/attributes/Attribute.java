package net.minecraft.entity.ai.attributes;

/**
 * Spawn compile stub: shape-only 1.16.5 (MCP) vanilla API used by
 * {@code forge/} sources. Never runs (compile classpath only). Only a
 * type token: the hp seam reads the max-health attribute through this
 * (measured in the pinned joined.tsrg — the 1.12 {@code IAttribute} name
 * does not port on 1.16.5).
 */
public class Attribute {
}
