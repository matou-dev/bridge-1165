package net.minecraft.block;

/**
 * E1 compile stub: shape-only 1.16.5 (MCP) vanilla API used by
 * {@code forge/} sources. Never runs (compile classpath only). Pinned
 * to the provisioned 1.16.5-36.2.42 jars by tools/run-live.sh (E3) —
 * drift fails loudly. Loot: extends
 * {@code AbstractBlock.AbstractBlockState} (measured in the pinned
 * joined.tsrg — {@code getBlock} is inherited, reads go through the
 * declaring type).
 */
public class BlockState extends AbstractBlock.AbstractBlockState {
}
