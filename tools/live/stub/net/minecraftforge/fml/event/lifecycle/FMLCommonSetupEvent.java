package net.minecraftforge.fml.event.lifecycle;

import net.minecraftforge.eventbus.api.Event;

/**
 * E0 registration stub: shape-only Forge 1.16.5-36.2.42 API (universal
 * jar, never obfuscated). Never runs (compile classpath only). Only the
 * type is referenced (setup-listener parameter) — no members. Presence
 * measured with javap against the provisioned universal jar, pinned by
 * tools/run-live.sh (live tranche) — drift fails loudly.
 */
public class FMLCommonSetupEvent extends Event {
}
