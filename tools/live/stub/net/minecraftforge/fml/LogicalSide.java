package net.minecraftforge.fml;

/**
 * E1 compile stub: shape-only Forge 1.16.5-36.2.42 API (fmlcore jar,
 * never obfuscated). Never runs (compile classpath only). Pinned by
 * tools/run-live.sh (E3) — drift fails loudly.
 */
public enum LogicalSide {
    CLIENT, SERVER
}
