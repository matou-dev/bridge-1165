package net.minecraftforge.api.distmarker;

/**
 * Custom entity compile stub: shape-only Forge 1.16.5-36.2.42 API
 * (forgespi 3.2.0 jar, never obfuscated). Never runs (compile classpath
 * only). Measured with javap against the provisioned forgespi jar
 * (enum with {@code CLIENT} + {@code DEDICATED_SERVER} — no bare
 * {@code SERVER} on this version), pinned in tools/run-live.sh —
 * drift fails loudly.
 */
public enum Dist {
    CLIENT, DEDICATED_SERVER
}
