package net.minecraftforge.registries;

import net.minecraft.block.Block;
import net.minecraft.entity.EntityType;

/**
 * E1 compile stub: shape-only Forge 1.16.5-36.2.42 API (universal jar,
 * never obfuscated). Never runs (compile classpath only). Member
 * {@code BLOCKS} is pinned by tools/run-live.sh (E3) — drift fails
 * loudly.
 *
 * <p>Custom entity tranche: {@code ENTITIES} carries the beast registry
 * (measured with javap against the provisioned universal jar, pinned
 * the same way — Forge classes are never obfuscated, presence is the
 * pin).
 */
public class ForgeRegistries {
    public static final IForgeRegistry<Block> BLOCKS = null;
    public static final IForgeRegistry<EntityType<?>> ENTITIES = null;
}
